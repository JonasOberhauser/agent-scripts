[package]
name = "gatekeeper-testkit"
version = "0.32.0"
edition = "2021"

[dependencies]
fuse-protocol = { path = "../fuse-protocol" }
fuse-server = { path = "../fuse-server" }
tempfile = "3"

[dev-dependencies]
libc = "0.2"

